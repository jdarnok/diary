<%= form_for(@user, :html => { :role => "form" }) do |f| %>

    <% if @user.errors.any? %>
        <div id="large-12 columns">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this@user from being saved:</h2>

          <ul>
            <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :first_name, :class => "control-label" %><br>
      <%= f.text_field :first_name, :class => "form-control" %>
    </div>
    <div class="row">
      <%= f.label :last_name, :class => "control-label" %><br>
      <%= f.text_field :last_name, :class => "form-control" %>
    </div>
    <div class="row">
      <%= f.label :school_class, :class => "control-label" %><br>
      <%= f.select(:school_class_id, options_from_collection_for_select(SchoolClass.all, 'id', 'name'), :class => "form-control") %>
    </div>
    <div class="row">
      <%= f.label :email, :class => "control-label" %><br>
      <%= f.text_field :email, :class => "form-control" %>
    </div>
    <div class="row">
      <%= f.label :email, :class => "control-label" %><br>
      <%= f.text_field :email, :class => "form-control" %>
    </div>

    <div class="row">
      <%= f.submit class: "button" %>
    </div>


<% end %>
